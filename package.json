{
  "name": "lakehouse-booking",
  "version": "1.0.0",
  "description": "Live booking site + admin portal: calendar availability, guests, ID uploads, signed agreement, time-off management.",
  "type": "module",
  "main": "server.js",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "build:preview": "node scripts/build-preview.mjs"
  },
  "dependencies": {
    "@libsql/client": "^0.14.0",
    "dotenv": "^16.4.5",
    "express": "^4.21.1"
  }
}
